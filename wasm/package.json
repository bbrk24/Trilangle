{
    "dependencies": {
        "coffeescript": "^2.7.0",
        "gh-pages": "^5.0.0",
        "sass": "^1.62.1"
    },
    "devDependencies": {
        "@coffeelint/cli": "^5.2.11",
        "express": "^4.18.0",
        "stylelint": "^15.6.2",
        "stylelint-config-standard-scss": "^9.0.0"
    },
    "scripts": {
        "start": "node -e \"(e => e().use(e.static('.')).listen(3000))(require('express'))\"",
        "lint": "stylelint ./*.scss && coffeelint in.coffee worker.coffee",
        "deploy": "gh-pages -d . -s \"{index.*,worker.js,worker.wasm,favicon.ico}\""
    },
    "engines": {
        "node": "^16.13.0 || >=18.0.0"
    }
}
