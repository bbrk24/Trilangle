{
    "dependencies": {
        "coffeescript": "^2.7.0",
        "sass": "^1.64.1"
    },
    "devDependencies": {
        "@coffeelint/cli": "^5.2.11",
        "clang-format": "^1.8.0",
        "express": "^4.18.0",
        "stylelint": "^15.10.2",
        "stylelint-config-standard-scss": "^10.0.0"
    },
    "scripts": {
        "start": "node -e \"(e => e().use(e.static('.')).listen(3000))(require('express'))\"",
        "lint": "stylelint ./*.scss && coffeelint in.coffee worker.coffee library.coffee && clang-format --Werror --dry-run ../src/*",
        "fix": "stylelint --fix ./*.scss; coffeelint --fix in.coffee worker.coffee library.coffee; clang-format -i ../src/*"
    },
    "engines": {
        "node": "^16.13.0 || >=18.0.0"
    }
}
