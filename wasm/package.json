{
    "dependencies": {
        "coffeescript": "^2.7.0",
        "sass": "^1.62.1"
    },
    "devDependencies": {
        "@coffeelint/cli": "^5.2.11",
        "express": "^4.18.0",
        "stylelint": "^15.10.1",
        "stylelint-config-standard-scss": "^10.0.0"
    },
    "scripts": {
        "start": "node -e \"(e => e().use(e.static('.')).listen(3000))(require('express'))\"",
        "lint": "stylelint ./*.scss && coffeelint in.coffee worker.coffee"
    },
    "engines": {
        "node": "^16.13.0 || >=18.0.0"
    }
}
