{
    "dependencies": {
        "express": "^4.18.0",
        "gh-pages": "^5.0.0",
        "sass": "^1.59.3",
        "stylelint": "~15.2.0",
        "stylelint-config-standard-scss": "^7.0.1"
    },
    "scripts": {
        "start": "node -e \"(e => e().use(e.static('.')).listen(3000))(require('express'))\"",
        "lint": "stylelint input.scss",
        "deploy": "gh-pages -d . -s \"{index.*,out.*,favicon.ico}\""
    }
}
